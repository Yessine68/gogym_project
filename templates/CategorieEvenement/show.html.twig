{% extends 'base1.html.twig' %}

{% block title %}Categorie{% endblock %}

{% block body %}
    <h1>Categorie</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ categorie.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ categorie.getNomCatE }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_categorieE_index') }}">back to list</a>

    <a href="{{ path('app_categorieE_edit', {'id': categorie.id}) }}">edit</a>

    {{ include('CategorieEvenement/_delete_form.html.twig') }}
{% endblock %}
