<form method="post" action="{{ path('lignecommande_delete', {'id_l_p': lignecommande.id_l_p}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ lignecommande.id_l_p) }}">
    <button class="btn btn-danger btn-sm">Delete</button>
</form>